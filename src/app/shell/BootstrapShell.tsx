import './bootstrap-shell.css';

export function BootstrapShell() {
  return (
    <section className="chatspace-bootstrap" aria-label="Chatspace extension status">
      <strong>Chatspace</strong>
      <span>Foundation ready</span>
    </section>
  );
}
